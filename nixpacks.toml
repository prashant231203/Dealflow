[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["cd supplier-api && pip install -r requirements.txt"]

[start]
cmd = "cd supplier-api && uvicorn main:app --host 0.0.0.0 --port $PORT"
